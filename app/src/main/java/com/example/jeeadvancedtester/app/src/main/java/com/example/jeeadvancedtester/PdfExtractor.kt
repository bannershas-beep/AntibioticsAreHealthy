```kotlin\npackage com.example.jeeadvancedtester\n\nimport android.util.Log\nimport org.apache.pdfbox.pdmodel.PDDocument\nimport org.apache.pdfbox.text.PDFTextStripper\nimport java.io.File\nimport java.util.regex.Pattern\n\ndata class Question(\n val id: Int,\n val stem: String,\n val options: Map<Char, String>,\n var rawAnswer: String = "",\n var type: String = ""\n)\n\nobject PdfExtractor {\n private const val TAG = "PdfExtractor"\n\n fun extractText(file: File): String {\n return if (file.name.endsWith(".pdf", ignoreCase = true)) {\n extractPdfText(file)\n } else {\n file.readText()\n }\n }\n\n private fun extractPdfText(pdfFile: File): String {\n return PDDocument.load(pdfFile).use { doc ->\n val stripper = PDFTextStripper()\n stripper.setLineSeparator("\\n")\n stripper.getText(doc)\n }\n }\n\n fun parseQuestions(rawText: String): List<Question> {\n val text = rawText.replace("\\r\\n", "\\n").trim()\n val blockPattern = Pattern.compile(\n """(?m)^\\s*(\\d+[\\.)])\\s+(.+?)(?=\\n\\s*[A-D][\\.\\)]) # number + stem\n \\n\\sA[\\.\\)]\\s+(.+?) # option A\n \\n\\sB[\\.\\)]\\s+(.+?) # option B\n \\n\\sC[\\.\\)]\\s+(.+?) # option C\n \\n\\sD[\\.\\)]\\s+(.+?) # option D\n \\n\\s*(?:Ans\\.?
